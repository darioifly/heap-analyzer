# F3.S01 — REPORT

## Commits pushed to main (4):
- `bbfb503` F3.S01: Python recompute_single_heap + split/merge + CLI commands
- `f612cf7` F3.S01: Electron IPC handlers for editing ops (atomic split/merge)
- `d00800f` F3.S01: EditingToolbar + PolygonEditor + editingStore + shortcuts + undo/redo
- `<this>` F3.S01: Tests + report

## Test counts:
- Python pytest:   127 → 145  (new: 7 in test_volume_recompute, 11 in test_polygon_ops, 1 in test_ipc_hygiene)
- Frontend vitest: 24 → 40 passing (new: 16 in editingStore.test.ts)
  - Note: 21 pre-existing test failures (window is not defined in test env) — unchanged
- Electron tsc:    clean (0 errors)
- Playwright:      deferred — requires running app (user tests via HMR)

## Regression guards active:
- [x] recompute_single_heap within +/-1% of compute_heap_metrics (test_volume_recompute.py)
- [x] No print() in python-engine source (test_ipc_hygiene.py)
- [x] Atomic split/merge via better-sqlite3 transaction (editing-handlers.ts)
- [x] Keyboard shortcuts disabled in text inputs (useEditingShortcuts.ts)
- [x] Undo history capped at 20 (editingStore.test.ts)

## Architecture summary:

### Python layer
- `volume.py`: Added `_compute_metrics_for_mask()` (single-heap vectorized) + `recompute_single_heap()`
- `polygon_ops.py`: New module with `split_polygon_by_line()` + `merge_polygons()` (Shapely 2.x)
- `cli.py`: 3 new commands: `recompute-heap`, `split-polygon`, `merge-polygons`

### Electron layer
- `editing-handlers.ts`: 6 IPC channels (createHeap, recomputeHeap, deleteHeap, splitHeap, mergeHeaps, restoreSnapshot)
- `db.ts`: Added `deleteHeap()`, `deleteHeapsBySurvey()`, `getDb()`
- `preload.ts`: `api.editing.*` namespace exposed

### Frontend layer
- `editingStore.ts`: Zustand store — tool state, undo/redo (20 max), merge multi-selection
- `EditingToolbar.tsx`: 6 tools + undo/redo, EVLOS styling, Italian tooltips with shortcuts
- `PolygonEditor.tsx`: OL Draw/Modify/Snap lifecycle per tool
- `EditingActions.tsx`: Delete AlertDialog + merge handler
- `useEditingShortcuts.ts`: V/P/M/X/U/Del/Esc/Ctrl+Z/Ctrl+Shift+Z (input-safe)
- `MapView.tsx`: Integrated all editing + heap vector layer + select/hover interactions

## Performance (manual E2E):
- Pending user verification via HMR (dev server already running)

## UX deviations (if any):
- None — all UX decisions from prompt implemented as specified

## Open questions for F3.S02:
- Base elevation override: currently reads from first heap's base_elevation. F3.S02 will add proper override UI.
- Ground polygon selection tool for terrain-based elevation estimation.
